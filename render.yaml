services:
  - type: web
    name: stegokiller
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 app.py"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: PORT
        value: 10000
